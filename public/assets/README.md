# Assets

Place production assets here before rendering:

- `audio/voiceover.mp3` — final narration
- `images/shot_001.jpg`, `shot_002.jpg`, ... — generated frames
- `music/*.mp3` — background music beds

The exact file paths and timings are defined in `src/edit-plan.json`.
